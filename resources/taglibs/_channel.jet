{{block channel(typeid=0,reid=0, type="son",row=100, noself=0)  }}
    {{ if typeid == 0 && isset(__refID) && __refID > -1}}
    {* __refID 继承channelartlist *}
        {{typeid = __refID}}
    {{end}}
    {{ categories := channel(typeid, reid, type, row, noself) }}
    {{if categories}}
        {{range autoindex,field = categories}}
            {{yield content}}
            {{__refID = -1}}
        {{end}}
    {{end}}
{{end}}
